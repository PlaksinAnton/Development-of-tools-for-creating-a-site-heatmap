const swiper = new Swiper('.swiper', {
  // Optional parameters
  loop: true,
  slidesPerView: 4,
  spaceBetween: 29,

  // If we need pagination
  pagination: {
    el: '.swiper-pagination',
    clickable: true,
  },
});